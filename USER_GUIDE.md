# מדריך למשתמש — FashionSync

## אודות המדריך

מדריך זה מסביר כיצד להשתמש במערכת FashionSync, הן מצד הלקוחה והן מצד מנהלת החנות, ומציג את הפעולות המרכזיות הזמינות בכל ממשק.

## תוכן עניינים

- [מדריך ללקוחה](#מדריך-ללקוחה)
- [ביצוע הזמנה — צעד אחר צעד](#ביצוע-הזמנה--צעד-אחר-צעד)
- [ביצוע בקשת החזרה — צעד אחר צעד](#ביצוע-בקשת-החזרה--צעד-אחר-צעד)
- [מדריך למנהלת](#מדריך-למנהלת)
- [טיפול בהזמנה — צעד אחר צעד (מנהלת)](#טיפול-בהזמנה--צעד-אחר-צעד-מנהלת)
- [הגדרות כלליות (שפה ומצב תצוגה)](#הגדרות-כלליות-שפה-ומצב-תצוגה)
- [שאלות נפוצות](#שאלות-נפוצות)
- [שימוש ב-Git במערכת](#שימוש-ב-git-במערכת)

---

## מדריך ללקוחה

### כניסה והרשמה
נכנסים מדף הבית, ואפשר להירשם עם אימייל וסיסמה, או לגלוש כאורחת. שימו לב: כאורחת אי אפשר להוסיף מוצרים לעגלה או לבצע הזמנה — לפעולות אלו נדרשת הרשמה.

### קטלוג, חיפוש וסינון
בעמוד הקטלוג ניתן לחפש מוצר לפי שם או לפי מספר הברקוד שלו (הקלדה ידנית), ולסנן לפי מגדר, קטגוריה, עונה ומחיר. ניתן לנווט בין המוצרים גם באמצעות מקלדת בלבד (Tab למעבר בין מוצרים, Enter לפתיחה, Escape לסגירת חלון).

### רשימת משאלות (Wishlist)
ניתן לסמן מוצרים אהובים ולשמור אותם לרשימה אישית, לצפייה וגישה מהירה בהמשך.

### הצ'אטבוט (SYNC)
עוזר חכם שיודע לענות על שאלות לגבי מוצרים, מחירים, מבצעים, מדיניות (החזרות, ביטול, משלוחים), שעות פעילות וכתובת החנות — לפי הנתונים העדכניים ביותר שהוגדרו במערכת. ניתן גם לבקש ממנו המלצת לוק, ולקבל הדמיה חזותית שלו.

### נאמנות, קופונים וכרטיסי מתנה
בעמוד "נקודות וקופונים" ניתן לראות את יתרת הנקודות שנצברה (נקודה אחת לכל ₪1), ואת הקופונים הזמינים. בעמוד "כרטיס מתנה" ניתן לבדוק יתרת כרטיס מתנה או זיכוי החזרה (קודי GC- ו-RTN-).

---

## ביצוע הזמנה — צעד אחר צעד

1. בוחרים מוצר מהקטלוג
2. בוחרים צבע ומידה
3. לוחצים "הוסף לעגלה"
4. פותחים את עגלת הקניות
5. עוברים לתהליך הזמנה (Checkout)
6. ממלאים פרטי משלוח (השדות ממולאים אוטומטית לפי החשבון המחובר)
7. בוחרים אמצעי משלוח — רגיל, מהיר, אותו יום, או איסוף עצמי
8. בוחרים אמצעי תשלום
9. מאשרים את ההזמנה
10. מתקבל מייל אישור, וניתן לעקוב אחר ההזמנה בעמוד "ההזמנות שלי"

---

## ביצוע בקשת החזרה — צעד אחר צעד

1. נכנסים לעמוד "ההזמנות שלי"
2. בוחרים את ההזמנה הרלוונטית (חייבת להיות במצב "נמסרה", בתוך 7 ימים ממועד המסירה)
3. לוחצים "בקשת החזרה"
4. בוחרים את הפריטים להחזרה
5. שולחים את הבקשה
6. המנהלת בודקת ומאשרת/דוחה את הבקשה
7. לאחר אישור, נוצר זיכוי אוטומטית (כרטיס מתנה בקוד RTN-)

**ביטול הזמנה** (לפני משלוח) אפשרי באופן עצמאי, ישירות מעמוד "ההזמנות שלי", תוך 24 שעות מרגע ביצוע ההזמנה.

---

## מדריך למנהלת

### כניסה לממשק הניהול
נכנסים דרך כתובת `/manager` באמצעות פרטי ההתחברות שהוגדרו עבור המנהלת.

### ניהול מלאי
הוספה ועריכה של מוצרים, כולל ניהול וריאנטים לפי צבע ומידה, תמונות, ומחירים. תרגום השם והתיאור לאנגלית מתבצע אוטומטית בעת השמירה.

### הזמנות
רשימת כל ההזמנות, עם אפשרות חיפוש לפי מספר הזמנה או טלפון. הזמנות שחורגות מזמן המשלוח המובטח מסומנות אוטומטית בתג "⏰ מתעכבת".

### משלוחים
עדכון סטטוס הזמנה לאורך תהליך המשלוח (אושרה → בהכנה → נשלחה/מוכן לאיסוף → נמסרה/נאסף).

### קבלות מכירה
פירוט מלא של כל עסקה שבוצעה בחנות.

### בקשות החזרה
אישור או דחייה של בקשות החזרה שהוגשו על ידי לקוחות, עם יצירת זיכוי אוטומטית באישור.

### פניות ומשוב
ניהול הודעות יצירת קשר ומשוב לקוחות שהתקבלו דרך עמוד המדיניות באתר.

### קופונים
יצירה וניהול של קודי הנחה.

### הגדרות חנות
עריכת תוכן מדיניות (משלוחים, החזרות, ביטולים, פרטיות), שעות פעילות, ופרטי חנות (כתובת, טלפון). כל שינוי כאן משפיע **מיידית** גם על הצ'אטבוט, ללא צורך בפעולה נוספת.

### דוחות ואנליטיקה
צפייה במגמות מכירה ונתוני ביצועים מבוססי הנתונים החיים במערכת.

---

## טיפול בהזמנה — צעד אחר צעד (מנהלת)

1. כניסה לממשק הניהול
2. פתיחת "הזמנות לקוחות"
3. בחירת ההזמנה הרלוונטית
4. בדיקת פרטי הלקוחה והפריטים
5. אישור ההזמנה (או דחייתה)
6. מעבר ל"מעקב משלוחים"
7. עדכון סטטוס המשלוח לפי שלב ההתקדמות בפועל

---

## הגדרות כלליות (שפה ומצב תצוגה)

בכל עמוד ניתן לעבור בין עברית לאנגלית (כפתור השפה), ובין מצב כהה לבהיר (כפתור המצב) — הבחירה נשמרת אוטומטית לפעם הבאה.

---

## שאלות נפוצות

**האם ניתן לבצע הזמנה ללא הרשמה?**
לא — גלישה כאורחת מאפשרת צפייה בקטלוג בלבד. הוספה לעגלה וביצוע הזמנה דורשים חשבון רשום.

**איך מבטלים הזמנה?**
בעמוד "ההזמנות שלי", תוך 24 שעות מביצוע ההזמנה ולפני שהיא נשלחה.

**איך מגישים בקשת החזרה?**
בעמוד "ההזמנות שלי", עד 7 ימים ממועד המסירה בפועל.

**איך עוברים בין עברית לאנגלית?**
לוחצים על כפתור השפה, הזמין בכל עמוד באתר.

**למה מוצר מסוים אינו זמין להזמנה?**
כאשר אין מלאי זמין בצבע/מידה שנבחרו, ניתן להירשם לקבלת התראה כאשר המוצר יחזור למלאי.

**שכחתי סיסמה — מה עושים?**
בעמוד ההתחברות יש אפשרות לאיפוס סיסמה באמצעות שליחת קישור לאימייל הרשום.

---

## שימוש ב-Git במערכת

הפרויקט מנוהל באמצעות Git — מערכת בקרת גרסאות ששומרת היסטוריה של כל שינוי בקוד, ומאפשרת לעבוד על תכונות שונות בנפרד (בענפים/Branches) מבלי לפגוע בגרסה היציבה.

### מושגי יסוד

|          מושג         |                        הסבר                                     |
|-----------------------|------------------------------------------------------------------|
| **Repository (Repo)** | תיקיית הפרויקט כולה, עם כל ההיסטוריה השמורה                   |
| **Commit**            | "תמונת מצב" שמורה של הקוד, עם הודעה שמסבירה מה השתנה          |
| **Branch (ענף)**      | קו עבודה נפרד — מאפשר לפתח תכונה חדשה בלי לגעת בגרסה הראשית  |
| **Push**              | העלאת השינויים השמורים (commits) לשרת המרוחק (GitHub)          |
| **Pull**              | הורדת שינויים עדכניים מהשרת המרוחק למחשב המקומי               |

### פקודות בסיסיות שימושיות

**יצירת ענף חדש ומעבר אליו:**
```bash
git checkout -b feature/שם-התכונה
```

**בדיקת אילו קבצים השתנו:**
```bash
git status
```

**שמירת השינויים (Commit):**
```bash
git add .
git commit -m "תיאור קצר וברור של מה שהשתנה"
```

**העלאת הענף לריפוזיטורי המרוחק:**
```bash
git push origin feature/שם-התכונה
```

**מעבר בין ענפים קיימים:**
```bash
git checkout שם-הענף
```

**הורדת עדכונים מהריפוזיטורי המרוחק:**
```bash
git pull
```

**מחיקת ענף מקומי (לאחר שלא צריך אותו יותר):**
```bash
git branch -D שם-הענף
```

### עקרון עבודה מומלץ בפרויקט

1. לפני התחלת עבודה על תכונה/תיקון חדש — ליצור **ענף חדש** (`git checkout -b ...`), לא לעבוד ישירות על הענף הראשי.
2. אחרי כל שינוי משמעותי — לבדוק שהקוד **נבנה בהצלחה** (`npm run build`) לפני שמירה.
3. לשמור (Commit) עם **הודעה ברורה** שמסבירה מה בדיוק תוקן/נוסף.
4. להעלות (`push`) את הענף לריפוזיטורי המרוחק בסיום כל קטע עבודה.
5. במידה ומשהו נשבר — ניתן תמיד לחזור לענף/commit קודם, כי שום דבר לא נמחק לצמיתות עד שמבצעים מיזוג (merge) לענף הראשי.